# GoT RAG — Strategy & Roadmap (placeholder)

> Drop the full strategy/roadmap doc here. Phase 0 references it from the repo
> layout; this placeholder keeps links valid until the real doc is added.

## Phase map (summary)

- **Phase 0** — setup & scaffold (this repo state).
- **Phase 1** — ingest real corpus; 384-dim vectors into Vectorize; wire `vectorStore` + `retrieval`.
- **Phase 2** — genealogy graph: curated seed + reviewed extraction; graph traversal in `retrieval`.
- **Phase 3** — model adapter live (Gemini 3 Flash default); LLM-as-judge eval.
- **Phase 4** — local Gemma via Ollama with auto-fallback.
- **Phase 5** — harden server-side auth (sessions, rate limiting).
- **Phase 6** — spoiler ceiling: per-chunk first-revealed-at annotation (critique 9).
