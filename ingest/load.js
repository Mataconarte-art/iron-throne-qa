// Phase 0 stub. load: upsert vectors + metadata into the vector store behind
// the same swappable interface the Function uses (functions/_lib/vectorStore.js).
console.log("[ingest:load] stub — Phase 1. Upserts vectors into Vectorize (or swapped backend).");
process.exit(0);
