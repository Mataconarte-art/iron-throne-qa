// Phase 0 stub. build-graph: start from the curated seed (../graph/seed-targaryen.json),
// then merge REVIEWED LLM-extracted edges. Curated data is the source of truth;
// a single wrong edge is what tanks genealogy accuracy (critique 3).
console.log("[ingest:build-graph] stub — Phase 1/2. Seeds from graph/seed-targaryen.json, adds reviewed edges.");
process.exit(0);
