// Phase 0 stub. chunk: normalized records -> ~few-hundred-token chunks, coarse
// side (fewer, larger chunks to respect the Vectorize dimension ceiling).
// Preserves and propagates all metadata tags onto every chunk.
console.log("[ingest:chunk] stub — Phase 1. Splits records into source-tagged chunks.");
process.exit(0);
