// Phase 0 stub. parse: source files (EPUB/PDF/TXT) in ./data -> normalized text
// records with provenance. Output feeds chunk.js.
// Record shape: { work, unit (book|season), section (chapter|episode), type, url, text }
console.log("[ingest:parse] stub — Phase 1. Reads ingest/data/, emits normalized text records.");
process.exit(0);
