# dependencies
node_modules/

# secrets
.dev.vars
.dev.vars.*
!.dev.vars.example

# source book text and ingest artifacts (copyright — never commit)
ingest/data/
ingest/data/**
ingest/out/
ingest/out/**
*.epub
*.pdf
*.mobi

# build / tooling
.wrangler/
dist/
.DS_Store
*.log

# derived graph artifacts (regenerated by `npm run ingest:graph` from
# graph/seed-targaryen.json + graph/reviewed-edges.json — both committed)
graph/graph.json
graph/edges.sql

# eval outputs
eval/results/*
!eval/results/.gitkeep
